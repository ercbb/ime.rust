[package]
name = "wheel-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = "1.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
slint-build = "1.9"
